[package]
name = "stabmouse-probe"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

# Throwaway-ish: this exists to answer the open platform questions in
# docs/modules.md before the real crates are written. The device handling here is
# expected to graduate into stabmouse-input / stabmouse-output.

[dependencies]
anyhow.workspace = true
clap.workspace = true
evdev.workspace = true
