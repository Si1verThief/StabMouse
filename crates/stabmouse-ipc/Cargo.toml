[package]
name = "stabmouse-ipc"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

# Shared D-Bus definitions. Depended on by the daemon (server), the CLI and the GUI
# (clients), so the wire contract exists in exactly one place and cannot drift between them.

[dependencies]
serde.workspace = true
thiserror.workspace = true
zbus.workspace = true
