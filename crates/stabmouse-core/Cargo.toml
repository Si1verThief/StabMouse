[package]
name = "stabmouse-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure filter pipeline for StabMouse. No I/O, no clock, deterministic."

# Deliberately dependency-free. This crate must build for wasm32 and through PyO3
# without feature gymnastics (see docs/modules.md), and every dependency added here
# is a dependency in three places.
[dependencies]

[dev-dependencies]
